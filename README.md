# basic-cudaQ-examples
A simple repository for CUDA-Q examples.

Since my machine is using Volta GPU (V100 16GB), I am unable to run the Python version of CUDAQ unlike Qiskit Aer, which means we will be stuck in C++.
